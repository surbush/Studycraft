name: studycraft
description: Subash
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.1.1
  sqflite: ^2.3.2
  path_provider: ^2.1.2
  csv: ^6.0.0
  flutter_local_notifications: ^17.0.0
  app_usage: ^3.0.2
  animated_tree_view: ^2.2.0
  sm2: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
